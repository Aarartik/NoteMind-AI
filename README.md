# NoteMind-AI
Developed an AI-based exam assistant that allows users to upload their study notes in PDF format and ask questions directly from the uploaded material using natural language queries. Implemented a Retrieval-Augmented Generation (RAG) pipeline where the PDF content is converted into embeddings, stored in a FAISS vector database.
